# 修复指令：Q2 + Q6

请立即修复以下2个问题：

## Q6（必改）：Day4 回收登记 - appraisedValue 前端校验

文件：`app/trade/new.tsx`

在提交回收单之前，对 appraisedValue（估价）增加前端校验：
1. 非空（必填）
2. > 0
3. 是合法数字（非 NaN）
4. 校验不通过时 Alert 提示，不发送请求

## Q2（补充）：Day1 日结 - paymentBreakdown 展示

文件：`app/settlement/index.tsx`

在日报Tab中，增加 paymentBreakdown 的展示：
- 如果后端返回了 paymentBreakdown（各支付渠道金额），用列表展示
- 例如：微信 ¥500 / 支付宝 ¥300 / 现金 ¥200
- 用已有的 `yuan()` 格式化金额
- 如果 paymentBreakdown 为空则不展示

## 完成后
- 确保 build 通过
- 不需要改其他文件
