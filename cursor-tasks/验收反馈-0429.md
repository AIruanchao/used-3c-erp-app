# 验收反馈 — 2026-04-29 13:24

锤锤完成Day1-6逐卡验收，以下是发现的问题和疑问。请逐条判断是否需要修改，给出结论和理由。

---

## Day1 日结

### Q1: NumericKeypad 未使用
- **现状**：确认日结Tab的金额输入用了 `TextInput` + `keyboardType="decimal-pad"`
- **任务卡原文**：「开班备用金」NumericKeypad 输入
- **疑问**：是否需要换成 NumericKeypad 组件？还是 TextInput 等价替代可以接受？

### Q2: paymentBreakdown 展示
- **现状**：日报Tab显示了总销售额/采购额/退款/支出，但 paymentBreakdown（各支付渠道金额）未在UI中展示
- **任务卡原文**：展示各支付渠道汇总
- **请确认**：是否需要增加 paymentBreakdown 的展示？

---

## Day2 交接班

### Q3: 接班确认功能
- **现状**：接班确认按钮未实现（任务卡审计时已知：后端只有 GET/POST，无 PATCH endpoint）
- **请确认**：当前跳过确认按钮是否合理？还是需要新建 PATCH endpoint？

---

## Day3 质保查询

### Q4: RepairOrder.deletedAt 过滤
- **现状**：warranty-service.ts 查询维修记录时是否排除了 deletedAt 不为 null 的记录？
- **请检查**：`GET /api/repair?sn=xxx` 是否带 where: { deletedAt: null } 条件

---

## Day4 回收登记

### Q5: TradeOrder 照片上传
- **现状**：trade/new.tsx 中照片上传用了 PhotoPickerGrid + 上传到 /api/upload
- **请确认**：上传后的 photoUrls 是否正确传入 createTradeOrder？

### Q6: appraisedValue 边界
- **现状**：appraisedValue 定义为必填 number
- **请确认**：前端是否有输入验证（>0、非空、非NaN）？

---

## Day5 通知栏

### Q7: ⚠️ audit-logs 同时用了 storeId 和 organizationId
- **现状**：`GET /api/audit-logs` 接口同时接受 `storeId` 和 `organizationId` 参数
- **任务卡原文**：AuditLog 没有 storeId 字段，只有 organizationId
- **疑问**：
  1. AuditLog 表里到底有没有 storeId 字段？请检查 schema.prisma
  2. 如果没有 storeId，API 接受这个参数就是无效过滤
  3. 如果有 storeId，那和任务卡不一致，请说明

### Q8: AlertLog 写入点
- **现状**：alert-trigger.ts 中写入点标注了 TODO
- **任务卡原文**：先做UI框架，预警数据后续补
- **请确认**：预警Tab当前是否展示空列表（EmptyState）？会不会报错？

### Q9: 3个Tab完整性
- **现状**：确认有「操作日志」「业务提醒」两个Tab
- **请确认**：「系统预警」Tab 是否存在？3个Tab是否齐全？

---

## Day6 找货卖货

### Q10: Device.skuId 为 null 时的型号名
- **现状**：marketplace search API 用 `d.Sku?.Model?.name ?? "未知型号"` 兜底
- **任务卡原文**：Device.skuId 是可选字段，可能为 null
- **请确认**：当 skuId=null 时，Sku 为 null，不会导致查询报错？

### Q11: 上架操作权限
- **任务卡原文**：上架/下架操作需要总部权限
- **请确认**：my-listings API 的上架/下架操作是否做了 isHeadquarters 权限校验？

### Q12: marketplace 双Tab
- **请确认**：找货（搜索）和 我的上架（管理）两个Tab是否完整？

---

## 通用问题

### Q13: 品牌色统一
- 请确认所有新增文件都使用 `BRAND_COLOR`（from lib/theme.ts），没有硬编码 #FFD700 或 #1890ff

### Q14: 权限校验
- 请确认所有新建的后端 API 都有 `getSessionUserId` + `requireOrgMember` 校验

---

## 请按以下格式回复

每条回复：
> **Q1**: 结论（✅ 无需改 / 🔧 需修改 / ⚠️ 已确认合理）+ 一句话理由
